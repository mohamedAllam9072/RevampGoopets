<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allam.revampgopets">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_butterfly"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_butterfly"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGopests2">
        <activity
            android:name=".ui.allChats.mainScreens.SelectContactActivity"
            android:exported="false" />
        <activity
            android:name="com.allam.revampgopets.ui.home.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>